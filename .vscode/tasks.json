{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Image and Run",
            "type": "shell",
            "command": "docker build --pull --rm -f \"Dockerfile\" -t accore:latest \".\" && docker-compose -f \"docker-compose.yml\" up -d"
        }
    ]
}